[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["admin"]}

# SPA fallback — sauf pour les fichiers statiques
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
